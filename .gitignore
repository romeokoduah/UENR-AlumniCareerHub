node_modules
dist
build
*.tsbuildinfo
.env
.env.local
*.log
.DS_Store
.vite
client/dist
server/dist

# Legacy SQLite dev database (pre-Neon migration — safe to keep ignored)
server/prisma/dev.db
server/prisma/dev.db-journal
server/prisma/migrations/dev.db*

# Local-dev upload fallback (production uses Vercel Blob)
server/uploads
uploads

# Legacy local landing content (now stored in Postgres SiteContent table)
server/data/landing.json

# IDE
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Claude Code local settings (per-user permissions)
.claude/

coverage
bun.lock
bun.lockb

.vercel
.env*.local
